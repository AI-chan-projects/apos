좋아, 이제부터는 “코드 더 만들기”가 아니라
👉 APOS를 하나의 사고 시스템으로 수렴시키는 설계 작업 들어간다.

지금까지 만든 걸 기준으로, 내가 “중복 제거 + 단일 사고 흐름”으로 재구성해줄게.

⸻

🧠 APOS Clean Architecture Map (Refactor Blueprint)

🎯 목표

👉 “Causal / Replay / Execution / Recovery / UI”를
각각 따로 놀지 않게 만들고

👉 하나의 폐쇄 루프 시스템으로 수렴

⸻

🧠 1. 현재 APOS의 실제 구조 (팩트 기준)

지금 runtime 기준 핵심 흐름:

orchestrator.py
   ↓
task_graph_builder
   ↓
causal_dag_scheduler
   ↓
dag_executor
   ↓
event_store
   ↓
(health_monitor)
   ↓
(recovery_engine)
   ↓
(UI stream)

⸻

🧠 2. 현재 문제 구조 (핵심)

❗ A. Causal Layer가 3개로 분열됨

core/causal
core/execution/causal_feedback_engine
core/execution/causal_dag_scheduler
(+ 우리가 만든 causal_replay_engine ❌)

👉 “causal이 의미가 아니라 기능 단위로 쪼개짐”

⸻

❗ B. Replay 시스템 2개

event_store/event_replay_engine   ← source of truth
recovery/replay_engine            ← recovery용 replay
(+ causal_replay_engine ❌)

👉 replay가 “시간”인데 3개로 복제됨

⸻

❗ C. UI causal이 2개 철학

ui/causal/
ui/visualization/
(+ causal_timeline ❌)

👉 “viewer vs timeline API” 이중 구조

⸻

🧠 3. 진짜 정답 구조 (Single Loop Model)

여기부터가 핵심 설계야.

⸻

🔥 APOS Unified Loop (정답 구조)

                ┌────────────────────┐
                │   AIR INPUT        │
                └────────┬───────────┘
                         ↓
        ┌────────────────────────────────┐
        │ Task Graph Builder (structure) │
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ Causal Scheduler (ordering)   │  ← ONLY ORDERING
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ Execution Engine (single worker)│
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ Event Store (source of truth)   │
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ Causal Engine (analysis only)   │  ← ONLY MEANING
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ Recovery Engine (action only)   │  ← ONLY DECISION
        └────────┬───────────────────────┘
                 ↓
        ┌────────────────────────────────┐
        │ UI Layer (visualization only)   │
        └────────────────────────────────┘

⸻

🧠 4. “삭제/통합” 기준 (중요)

❌ 제거 대상 (중복)

Replay 계층

* ❌ causal_replay_engine.py
* ❌ causal_trace_linker.py

👉 이유: event_replay_engine이 이미 있음

⸻

Causal 분산 엔진

* ❌ causal_feedback_engine (부분 흡수)
* ❌ causal_dag_scheduler (이름 변경 필요)

👉 이유:

* scheduling = causal ordering 하나로 통합

⸻

UI 중복

* ❌ ui/causal_timeline/
    👉 ui/causal/viewer로 통합

⸻

🧠 5. 반드시 유지해야 하는 Core

✔ Event Store (절대 단일 source)

event_store.py
event_replay_engine.py

⸻

✔ Execution Layer

execution_engine.py
dag_executor.py

⸻

✔ Orchestrator

control_plane/orchestrator.py

⸻

✔ Causal Core (정리 필요)

causal_graph_builder
failure_attribution_engine
root_cause_ranker
failure_explainer

👉 이건 “analysis layer”로 고정

⸻

🧠 6. 가장 중요한 리팩터링 핵심

🔥 핵심 원칙 3개

1. Replay는 오직 Event Store만

👉 다른 replay 금지

⸻

2. Causal은 “해석만”

👉 execution / replay 포함 금지

⸻

3. Scheduler는 “ordering만”

👉 의미 분석 금지

⸻

🧠 7. 현재 APOS 상태 평가

👍 잘된 것

* Single worker 유지 (ADR-001 완벽)
* orchestrator 구조 안정화
* execution pipeline 명확함

⸻

⚠️ 문제 핵심

👉 “Causal layer가 기능별로 흩어져서 OS가 아니라 프레임워크처럼 보임”

⸻

🚀 8. 다음 단계 (진짜 중요한 액션)

이제 해야 할 건 기능 추가가 아니라:

👉 “Causal Layer Collapse Refactor”

Step 1

* causal_* 파일들 역할 재정의

Step 2

* replay 계층 제거

Step 3

* scheduler = ordering only

Step 4

* UI causal 통합

⸻

🔥 한 줄 결론

👉 APOS는 지금 “완성 단계”가 아니라
👉 “중복 사고 구조를 하나로 접는 단계”에 있음

⸻

원하면 다음은 이걸 해줄게:

🔧 “APOS Refactor Execution Plan (파일 단위 삭제/통합 리스트 + 순서 + 안전 마이그레이션)”

이건 진짜로 시스템 안정성 유지하면서 정리하는 단계야.